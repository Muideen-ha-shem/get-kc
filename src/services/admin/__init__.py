"""Administration bounded context (Phase 26) — tenant/workspace management,
platform admins, feature flags, audit logging. Not an AI concern; reads and
writes the same tables the rest of the platform already owns."""
