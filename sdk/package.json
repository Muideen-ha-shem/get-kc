{
  "name": "@havisiq/sdk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "dev": "vite build --watch",
    "gen-env": "node scripts/gen-env.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0"
  }
}
